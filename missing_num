class Sol:
    def missing_num(self,arr):
        sum=0
        l=len(arr)
        for i in arr:
            sum+=i
        t_sum=(l+1)*l/2
        missing_num=int(t_sum-sum)
        return missing_num

if __name__ == '__main__':
    p1=Sol()
    print(p1.missing_num([0,1,3,2,4,5,6,7,9]))
